version: 2
updates:
    - package-ecosystem: pip
      directory: "/"
      schedule:
          interval: daily
          time: "06:00"
          timezone: Etc/UTC
      groups:
          python-minor-and-patch:
              update-types:
                  - minor
                  - patch

    - package-ecosystem: npm
      directory: "/"
      schedule:
          interval: daily
          time: "06:00"
          timezone: Etc/UTC
      groups:
          # Vitest and its coverage provider require matching releases, including majors.
          vitest:
              patterns:
                  - vitest
                  - "@vitest/*"
          npm-minor-and-patch:
              update-types:
                  - minor
                  - patch

    - package-ecosystem: github-actions
      directory: "/"
      schedule:
          interval: daily
          time: "06:00"
          timezone: Etc/UTC
      groups:
          actions-all:
              patterns:
                  - "*"
