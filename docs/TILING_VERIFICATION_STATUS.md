# Tiling Verification Status

This file tracks the current verification level for every tiling family in the app.

Implementation-status contracts for aperiodic families live in `backend/simulation/aperiodic_contracts.py`. The developer-facing strength report includes those statuses beside verification coverage, so promotion decisions can distinguish true substitutions, exact-affine paths, canonical patches, and known deviations.

Legend:

- `Geometric sanity`: passes `python -m tools tilings validate`
- `Literature verification`: passes `python -m tools tilings verify`
- `Strength`: how strong the current literature check is

## Current Status

| Group | Geometries | Geometric sanity | Literature verification | Strength | Notes |
| --- | --- | --- | --- | --- | --- |
| Regular grids | `square`, `hex`, `triangle` | PASS | PASS | Sample-level exact | Verified on canonical open-boundary `3x3` samples with exact totals, adjacency pairs, degree histograms, and signatures. |
| Classic aperiodic | `penrose-p3-rhombs`, `penrose-p3-rhombs-vertex`, `penrose-p2-kite-dart`, `ammann-beenker` | PASS | PASS | Patch-level exact | Verified by patch-depth counts, allowed kinds, adjacency invariants, and deterministic signatures. |
| Classic aperiodic | `penrose-p1-pentagon-diamond`, `penrose-p1-pentagon-boat-star` | PASS | PASS | Patch-level exact + public-kind/adjacency checks | Verified by deterministic canonical patch samples with exact totals, expected public cell-kind counts, adjacency pairs, degree histograms, and stable signatures. The distributed pentagon-diamond family and the centered pentagon-boat-star family are tracked separately so release metadata stays aligned with the actual emitted P1 prototiles. |
| Classic aperiodic | `socolar-12-fold` | PASS | PASS | Patch-level exact + public-kind/adjacency checks | Dodecagonal rhomb tiling (rhombus variant of the Socolar tiling, MLD to `shield`) built by the de Bruijn generalized-dual multigrid over six 30-degree-spaced families. Verified by exact patch-depth counts (44/102/250/623), expected rhomb-kind counts, adjacency pairs, and deterministic signatures; a focused unit test also checks the independent geometric invariants (equilateral rhombi, 30/60/90 angles, edges on the 30-degree star). Not the published marked substitution -- see [TILING_KNOWN_DEVIATIONS.md](TILING_KNOWN_DEVIATIONS.md). |
| Classic aperiodic | `enneagonal-9-fold` | PASS | PASS | Patch-level exact + public-kind/adjacency checks | The catalog's first 9-fold tiling: the de Bruijn enneagrid rhomb tiling (9-fold analogue of the Penrose and Socolar multigrid rhomb tilings) built over nine 20-degree-spaced families. Verified by exact patch-depth counts (62/137/292/641), expected rhomb-kind counts, adjacency pairs, and deterministic signatures; a focused unit test also checks the independent geometric invariants (equilateral rhombi, 20/40/60/80 acute angles, edges on the 9-fold star). Not a marked substitution -- see [TILING_KNOWN_DEVIATIONS.md](TILING_KNOWN_DEVIATIONS.md). |
| Classic aperiodic | `heptagonal-7-fold` | PASS | PASS | Patch-level exact + public-kind/adjacency checks | The catalog's first 7-fold tiling: the de Bruijn heptagrid rhomb tiling (7-fold analogue of the Penrose and Socolar multigrid rhomb tilings) built over seven `2*pi/7`-spaced families. Verified by exact patch-depth counts (62/139/317/707), expected rhomb-kind counts, adjacency pairs, and deterministic signatures; a focused unit test also checks the independent geometric invariants (equilateral rhombi, `pi/7`/`2*pi/7`/`3*pi/7` acute angles, edges on the 7-fold star). Not the Goodman-Strauss marked substitution -- see [TILING_KNOWN_DEVIATIONS.md](TILING_KNOWN_DEVIATIONS.md). |
| Classic aperiodic | `hendecagonal-11-fold` | PASS | PASS | Patch-level exact + public-kind/adjacency checks | The catalog's first 11-fold tiling (and first prime symmetry beyond 7): the de Bruijn hendecagrid rhomb tiling built over eleven `2*pi/11`-spaced families. Verified by exact patch-depth counts (57/127/268/634), expected rhomb-kind counts, all fifteen adjacency pairs, and deterministic signatures; a focused unit test also checks the independent geometric invariants (equilateral rhombi, `k * 180/11` acute angles for k = 1..5, edges on the 11-fold star). Not a marked substitution -- see [TILING_KNOWN_DEVIATIONS.md](TILING_KNOWN_DEVIATIONS.md). |
| Classic aperiodic | `tridecagonal-13-fold` | PASS | PASS | Patch-level exact + public-kind/adjacency checks | The catalog's first 13-fold tiling: the de Bruijn tridecagrid rhomb tiling built over thirteen `2*pi/13`-spaced families (13 is prime, so all families are independent). Verified by exact patch-depth counts (72/150/317/707), expected rhomb-kind counts, the 20 realized adjacency pairs (all but rhomb-4/rhomb-4 in the cropped patches), and deterministic signatures; a focused unit test also checks the independent geometric invariants (equilateral rhombi, `k * 180/13` acute angles for k = 1..6, edges on the 13-fold star, connected depth-0 patch). Not a marked substitution -- see [TILING_KNOWN_DEVIATIONS.md](TILING_KNOWN_DEVIATIONS.md). |
| Newer substitution aperiodic | `spectre`, `taylor-socolar`, `sphinx` | PASS | PASS | Mixed, canonical-patch + representative overlap/render-bounds + browser-visible render check | Still mostly based on low-depth counts, adjacency, signatures, and contiguity checks rather than full substitution-matrix proofs, but they now also have exact depth-3 canonical patch fixtures, representative render-bounds and adapter-space overlap fixtures, and settled browser-visible occupancy checks. They still do not have the richer multicolor/browser-review gates used for the most scrutinized families. |
| Newer substitution aperiodic | `robinson-triangles`, `tuebingen-triangle` | PASS | PASS | Mixed, metadata + canonical-patch + representative overlap/render-bounds + browser-visible render check | Verified by low-depth counts, metadata, adjacency, deterministic signatures, direct canonical patch fixtures at depth `1` and depth `3`, representative render-bounds fixtures, representative adapter-space overlap checks, and browser-visible Playwright gates that wait for the final settled patch and require strong canvas occupancy plus multiple dead-state fill colors. |
| Newer substitution aperiodic | `chair` | PASS | PASS | Mixed, metadata + local-reference + browser-visible render check | Verified as a true chair substitution through exact depth totals, orientation-token metadata, rooted local-reference fixtures, strict validation, render-bounds coverage, and a browser-visible Playwright check that the patch fills the viewport sensibly and exposes the four orientation-based dead-state colors. |
| Newer substitution aperiodic | `hat-monotile` | PASS | PASS | Mixed, local-reference + browser-visible render check | Hat is connected, overlap-clean, and hole-free under the canonical sample checks, preserves the reflected-neighbor chirality pattern used by the literature verifier, matches a checked-in rooted local-reference fixture, and now also has a browser-visible Playwright gate for settled multi-fill occupancy. |
| Newer substitution aperiodic | `turtle-monotile` | PASS | PASS | Mixed, exact hat-continuum deformation + canonical-outline congruence | Turtle is the `Tile(sqrt(3), 1)` member of the hat continuum, built as the exact per-edge-class deformation of the verified hat tiling. It therefore shares the hat's metatile substitution structure and adjacency at every depth: connected, overlap-clean, and hole-free under the canonical sample checks, with the same reflected-neighbor chirality pattern enforced by the literature verifier. A focused unit test additionally checks each tile against the independent canonical turtle outline (edge-length and turn-angle multisets) from `christianp/aperiodic-monotile`. It does not yet have its own rooted local-reference fixture or a turtle-specific browser-visible Playwright gate. |
| Newer substitution aperiodic | `dodecagonal-square-triangle` | PASS | PASS | Mixed, decorated 3.12.12 generator + canonical-patch + browser-visible render check | Runtime is a decorated 3.12.12 Archimedean tiling: hexagonal lattice of regular dodecagonal supercells decomposed into the canonical six unit squares plus twelve unit equilateral triangles, with two bridging triangles per supercell from the underlying 3.12.12 layout. Tiles the plane exactly, scales without depth limit, depends on no vendored data. Verified by exact public kind counts, expected adjacency pairs, exact degree histograms, exact canonical patch fixtures at depth `1` and depth `3`, representative render-bounds coverage, and a settled-render Playwright gate. This is not the canonical Schlottmann quasi-periodic square-triangle tiling but it preserves the family's dodecagonal flavour. |
| Newer substitution aperiodic | `shield` | PASS | PASS | Mixed, exact marked substitution + canonical-patch + local-reference + browser-visible render check | Verified from Gahler's exact marked recursive shield rule translated out of the published PostScript source, with strict shared-surface validation, rooted local-reference anchors, exact canonical patch fixtures at depth `1` and depth `3`, representative render-bounds coverage, and a settled-render Playwright gate. Manual visual review accepted the promoted patch on April 19, 2026, so `shield` now ships in the main `Aperiodic` group. |
| Newer substitution aperiodic | `pinwheel` | PASS | PASS | Mixed, exact-path + canonical-patch + local-reference + browser-visible render check | Verified on the exact-affine path from a two-triangle literature seed forming a rectangle, with `2 * 5^d` exact-record growth, root-id parity between exact records and runtime cells, expanding support, a rooted local-reference anchor, exact canonical patch fixtures including ids at depth `1` and depth `3`, strict validation, representative render-bounds coverage, and a settled-render Playwright gate. A June 11, 2026 literature comparison found and fixed a depth-2+ subdivision shear (three child tuples in non-canonical vertex order); every tile is now congruent `1:2:sqrt(5)` and the verifier enforces per-tile congruence. Manual visual review accepted the corrected congruent patch on June 12, 2026, so `pinwheel` now ships in the main `Aperiodic` group. |
| Periodic mixed / periodic-face | `archimedean-4-8-8`, `archimedean-3-12-12`, `archimedean-3-4-6-4`, `archimedean-4-6-12`, `archimedean-3-3-4-3-4`, `archimedean-3-3-3-4-4`, `archimedean-3-3-3-3-6`, `trihexagonal-3-6-3-6`, `cairo-pentagonal`, `rhombille`, `deltoidal-hexagonal`, `tetrakis-square`, `triakis-triangular`, `deltoidal-trihexagonal`, `prismatic-pentagonal`, `floret-pentagonal`, `type-7-pentagonal`, `house-pentagonal`, `snub-square-dual`, `kisrhombille` | PASS | PASS | Sample-level exact + descriptor semantics + interior vertex stars/frequencies + dual checks | Verified on family-specific canonical periodic samples defined in the reference specs. The current audit across `3x3`, `4x4`, and `5x5` kept every shipped periodic family on `3x3`, while still adding explicit sample-size override support, exact interior vertex-configuration sets, exact interior vertex-configuration frequencies, reciprocal dual-structure checks for unambiguous pairs, and candidate-class dual-signature checks where the current catalog is structurally ambiguous. |

## Next Up

- The current direct canonical fixture layer now covers `spectre`, `taylor-socolar`, `sphinx`, `robinson-triangles`, `tuebingen-triangle`, `dodecagonal-square-triangle`, `shield`, and `pinwheel`, with shallow depth-`1` fixtures added for the higher-risk aperiodic families; keep future expansions selective and only where the extra exactness buys materially stronger guarantees.
- Add new verified tilings such as another substitution family like `socolar-12-fold`.
- If a faithful canonical Schlottmann marked-prototile substitution becomes available, swap the decorated-3.12.12 runtime for `dodecagonal-square-triangle`. The current generator is correct as a square-triangle tiling but is periodic, not quasi-periodic.
